# Add the task ↔ Change integration plugin.
# dsh-task-orchestrator and dsh-change-control are loaded separately by the profile bundle.
- insert:
    - id: task-change-control
      name: dsh-task-change-control
